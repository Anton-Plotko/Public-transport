@page
@model Public_transport.Pages.TrolleybusesModel
@{
    int i = 1;
    int a = 1;
}
<div class="text-center">
    <h1 class="display-6">Выберите Троллейбус!</h1>
    <ul style="list-style: none;">
    @foreach (PublicTransport Troll in Model.ResTroll)
    {
            <li><a class="navbar-brand" href="/Stops?IdTransport=@a">@Troll.Name</a></li>
        if (i % 2 == 0)
            {<br>
            }
            ++i;
            ++a;
        }
    </ul>
</div>